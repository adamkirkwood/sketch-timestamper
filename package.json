{
	"name": "sketch-timestamper",
	"version": "1.0.0",
	"description": "Set text layers with timestamps at discrete time intervals",
	"main": "timestamper.sketchplugin",
	"repository": "https://github.com/adamkirkwood/sketch-timestamper",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "skpm build -w",
		"build": "skpm build",
		"publish": "skpm publish"
	},
	"keywords": [
		"sketch",
		"sketch",
		"plugin"
	],
	"author": "Adam Kirkwood <hello@adamkirkwood.com>",
	"license": "MIT",
	"engines": {
		"sketch": ">=3.0"
	},
	"manifest": "src/manifest.json",
	"devDependencies": {
		"babel-preset-es2015": "^6.24.1",
		"skpm": "^0.9.14"
	},
	"dependencies": {
		"discrete-time": "^0.2.1",
		"moment": "^2.18.1"
	}
}
